# -*- coding: utf-8 -*-
# vim: ft=yaml

bootstrap:
  issue: /etc/issue
  issue_net: /etc/issue.net
  motd: /etc/motd
  skel: /etc/skel
  bash_bashrc: /etc/bash.bashrc
  netdata_pkg: netdata
  netdata_conf_dir: /etc/netdata/
  netdata_port: 19999
  auto: True
