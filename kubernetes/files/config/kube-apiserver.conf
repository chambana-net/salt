###
# This file is generated by Saltstack, do not edit. 
#
# The following values are used to configure the kube-apiserver
#

KUBE_APISERVER_OPTS="\
 --insecure-bind-address="{{ insecure_bind_address }}" \
 --insecure-port="{{ insecure_port }}" \
 --etcd-servers="{{ etcd_servers }}" \
 --logtostderr="true" \
 --service-cluster-ip-range="{{ service_cluster_ip_range or '' }}" \
 --admission-control="{{ admission_control or '' }}" \
 --service-node-port-range="{{ service_node_port_range or '' }}" \
 --advertise-address="{{ advertise_address or '' }}" \
 --client-ca-file="{{ client_ca_file }}" \
 --tls-cert-file="{{ tls_cert_file }}" \
 --tls-private-key-file="{{ tls_private_key_file }}" \
 {{ other_opts or '' }}"
